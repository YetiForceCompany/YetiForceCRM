'use strict';

/* {[The file is published on the basis of YetiForce Public License 3.0 that can be found in the following directory: licenses/LicenseEN.txt or yetiforce.com]} */

Vtiger_RelatedList_Js('PriceBooks_RelatedList_Js', {
	triggerMassMargin: function triggerMassMargin() {
		var self = Vtiger_RelatedList_Js.relatedListInstance;
		var selected_ids = self.readSelectedIds(true),
		    excluded_ids = self.readExcludedIds(true);
		if (self.checkListRecordSelected() !== true) {
			app.showModalWindow({
				url: 'index.php?module=PriceBooks&view=SpecifyMargin',
				cb: function cb(modalContainer) {
					modalContainer.find('.js-modal__save').on('click', function (e) {
						var element = modalContainer.find('.js-margin');
						var resultOfValidation = Vtiger_NumberUserFormat_Validator_Js.invokeValidation(element);
						if (typeof resultOfValidation !== 'undefined') {
							element.validationEngine('showPrompt', resultOfValidation, '', 'topLeft', true);
							e.preventDefault();
						} else {
							element.validationEngine('hideAll');
							var postData = self.getCompleteParams();
							delete postData.view;
							postData.selected_ids = selected_ids;
							postData.excluded_ids = excluded_ids;
							postData.mode = 'specifyMargin';
							postData.action = 'RelationAjax';
							postData.relatedModule = self.moduleName;
							postData.record = self.getParentId();
							postData.margin = element.val();
							AppConnector.request(postData).done(function (response) {
								if (response.success) {
									Vtiger_Detail_Js.reloadRelatedList();
									app.hideModalWindow();
								}
							});
						}
						return false;
					});
				}
			});
		} else {
			self.noRecordSelectedAlert();
		}
	}
}, {
	registerEditListPrice: function registerEditListPrice() {
		var thisInstance = this;
		var element = this.content.find('.js-edit-listprice');
		element.validationEngine(app.validationEngineOptions);
		element.on('change', function (e) {
			e.stopPropagation();
			var element = $(e.currentTarget);
			element.formatNumber();
			if (!element.validationEngine('validate')) {
				AppConnector.request({
					module: thisInstance.parentModuleName,
					record: element.closest('.js-list__row').data('id'),
					action: 'RelationAjax',
					mode: 'addListPrice',
					src_record: thisInstance.parentRecordId,
					related_module: thisInstance.moduleName,
					price: element.val()
				}).done(function (responseData) {
					if (responseData.result) {
						Vtiger_Helper_Js.showPnotify({ text: app.vtranslate('JS_SAVE_NOTIFY_OK'), type: 'success' });
					}
				});
			}
		});
	},
	registerPostLoadEvents: function registerPostLoadEvents() {
		this._super();
		this.registerEditListPrice();
	}
});
//# sourceMappingURL=RelatedList.min.js.map

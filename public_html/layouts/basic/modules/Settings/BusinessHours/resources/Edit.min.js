'use strict';

$.Class('Settings_BusinessHours_Edit_Js',{},{/**
		 * Function to register form for validation
		 */registerFormForValidation:function registerFormForValidation(container){container.validationEngine(app.validationEngineOptions);},/**
		 * Function which will handle the registrations for the elements
		 */registerEvents:function registerEvents(){var container=$('#EditView');this.registerFormForValidation(container),app.registerEventForClockPicker(),App.Fields.TimePeriod.register(container);}});
//# sourceMappingURL=Edit.min.js.map
